document.addEventListener('DOMContentLoaded', () => {
    // let allD = document.querySelectorAll('main article');
    // allD.forEach((item) => {
    //     let degValue = 0;
    //     item.addEventListener('click', (e) => {
    //         if (!e.target.closest('details')) {
    //             item.querySelector('.cube__wrapper').style.transform = `rotateY(${degValue + 90}deg)`;
    //             degValue += 90;

    //         }
    //     });
    // });
});